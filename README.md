A dead-simple template server. Fetch config file templates via URL. 

Hosted on Cloudflare Pages at `tmpl.navin.codes`.

## Usage

```sh
# Fetch via curl
curl tmpl.navin.codes/docker-compose
curl tmpl.navin.codes/docker-compose/mysql
curl tmpl.navin.codes/traefik
```

